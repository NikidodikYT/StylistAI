# StyleSnap (MVP)

StyleSnap --- это fullstack-приложение персонального AI-стилиста.

Проект позволяет: - Загружать одежду в цифровой гардероб - Хранить вещи
в базе данных - Генерировать образы (через AI) - Управлять профилем
пользователя - Работать с аутентификацией (JWT)

⚠️ Это MVP-версия проекта. Часть AI-функционала может требовать VPN
(Gemini API ограничения по региону).

------------------------------------------------------------------------

## 🚀 Технологии

### Backend

-   FastAPI
-   PostgreSQL
-   SQLAlchemy (async)
-   Docker + Docker Compose
-   JWT Authentication
-   Gemini API (AI генерация образов)

### Frontend

-   Next.js 16 (App Router)
-   React
-   TypeScript
-   TailwindCSS
-   Zustand (persist в localStorage)
-   shadcn/ui

------------------------------------------------------------------------

## ⚙️ Требования

Перед запуском убедитесь, что установлены:

-   Docker + Docker Compose
-   Node.js 18+
-   npm

------------------------------------------------------------------------

# 🔧 Запуск проекта

## 1️⃣ Backend

Перейдите в папку backend:

``` bash
cd backend
docker-compose up --build
```

Backend будет доступен по адресу:

http://localhost:8000

Swagger-документация:

http://localhost:8000/docs

------------------------------------------------------------------------

## 2️⃣ Frontend

Перейдите в папку frontend:

``` bash
cd frontend
npm install
npm run dev
```

Frontend будет доступен по адресу:

http://localhost:3000

------------------------------------------------------------------------

# 🔐 Переменные окружения (.env для backend)

Пример `.env`:

DATABASE_URL=postgresql+asyncpg://stylist:password123@db:5432/stylist_db
SECRET_KEY=supersecretkey GEMINI_API_KEY=your_gemini_key

⚠️ Для работы AI-функционала может потребоваться VPN.

------------------------------------------------------------------------

# 📦 Основной функционал MVP

-   Регистрация / Авторизация
-   Загрузка одежды (сохранение фото на сервере)
-   CRUD гардероба
-   Генерация образов через AI
-   Сохранение образов в коллекции
-   JWT-аутентификация

------------------------------------------------------------------------

# 🧠 Ограничения MVP

-   AI зависит от Gemini API (региональные ограничения)
-   Нет production-конфигурации
-   Нет полноценной системы рекомендаций (демо-логика)

------------------------------------------------------------------------

# 📌 Статус проекта

MVP версия.\
Планируется доработка:

-   Улучшенная AI-логика
-   Улучшение UX
-   Production деплой
-   Тестирование
-   Оптимизация производительности

------------------------------------------------------------------------

# 🧑‍💻 Автор

Воробьев Никита 10А
Илья Зонис 10А
